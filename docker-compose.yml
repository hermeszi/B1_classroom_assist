services:
  app:
    build: .
    ports:
      - "8001:8001"
    env_file: .env
    volumes:
      - ./src:/app/src     # hot edits during dev
      - ./data:/app/data   # SQLite persistence
    command: uvicorn src.main:app --host 0.0.0.0 --port 8001 --reload
